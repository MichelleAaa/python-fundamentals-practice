my_tuple = (1, 2, 3, 4, 5)

print(my_tuple[0] + my_tuple[1])

#my_tuple[3] = 1 - not able to do this as tuples are immutable.

my_tuple = (5, 4, 3, 2, 1) # this overrides the previous value of the tuple to this new one. We can overwrite the entire tuple only, we can't change an individual value in it.
print(my_tuple)

